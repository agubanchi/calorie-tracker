import type { Activity } from "../types"

type CalorieTrackersProps ={
    activities: Activity[]
}

export default function CalorieTracker({actvities}:CalorieTrackersProps) {
  return (
    <div>CalorieTracker</div>
  )
}
